# RBAC Architecture Diagrams

**Project:** Industrial Pollution Monitoring System  
**Date:** May 6, 2026

---

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                             │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  React Frontend (Port 3000)                              │   │
│  │  ┌────────────────┐  ┌────────────────┐                 │   │
│  │  │ ProtectedRoute │  │ PermissionGate │                 │   │
│  │  │  - Auth check  │  │  - UI hiding   │                 │   │
│  │  │  - Role check  │  │  - Permission  │                 │   │
│  │  │  - Permission  │  │    checks      │                 │   │
│  │  └────────────────┘  └────────────────┘                 │   │
│  │           │                    │                         │   │
│  │           └────────┬───────────┘                         │   │
│  │                    ▼                                     │   │
│  │         ┌──────────────────────┐                        │   │
│  │         │  RBAC Check Logic    │                        │   │
│  │         │  checkPermission.ts  │                        │   │
│  │         └──────────────────────┘                        │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP + JWT
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         API LAYER                                │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Express Backend (Port 5000)                             │   │
│  │                                                           │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │   │
│  │  │ verifyToken │→ │  checkRole  │→ │  checkZone  │     │   │
│  │  │             │  │             │  │             │     │   │
│  │  │ - Extract   │  │ - Validate  │  │ - Filter    │     │   │
│  │  │   JWT       │  │   role      │  │   by zone   │     │   │
│  │  │ - Validate  │  │ - Check     │  │   (OPERATOR)│     │   │
│  │  │   signature │  │   hierarchy │  │             │     │   │
│  │  │ - Check     │  │ - Verify    │  │             │     │   │
│  │  │   expiry    │  │   permission│  │             │     │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘     │   │
│  │         │                 │                 │            │   │
│  │         └────────┬────────┴────────┬────────┘            │   │
│  │                  ▼                 ▼                     │   │
│  │         ┌──────────────┐  ┌──────────────┐             │   │
│  │         │ Controllers  │  │   Services   │             │   │
│  │         │              │  │              │             │   │
│  │         │ - HTTP logic │→ │ - Business   │             │   │
│  │         │ - Validation │  │   logic      │             │   │
│  │         │              │  │ - RBAC       │             │   │
│  │         │              │  │   enforcement│             │   │
│  │         └──────────────┘  └──────────────┘             │   │
│  │                                   │                     │   │
│  │                                   ▼                     │   │
│  │                          ┌──────────────┐              │   │
│  │                          │ Repositories │              │   │
│  │                          │              │              │   │
│  │                          │ - DB queries │              │   │
│  │                          │ - Data access│              │   │
│  │                          └──────────────┘              │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         DATA LAYER                               │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  MongoDB                                                  │   │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐           │   │
│  │  │  User  │ │  Site  │ │  Zone  │ │ Sensor │           │   │
│  │  │        │ │        │ │        │ │  Node  │           │   │
│  │  │ - role │ │ - super│ │ - site │ │ - zone │           │   │
│  │  │ - sites│ │   visor│ │ - opera│ │        │           │   │
│  │  │ - zones│ │        │ │   tors  │ │        │           │   │
│  │  └────────┘ └────────┘ └────────┘ └────────┘           │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Authentication Flow

```
┌──────────┐                                              ┌──────────┐
│  Client  │                                              │  Server  │
└────┬─────┘                                              └────┬─────┘
     │                                                          │
     │  1. POST /api/auth/login                                │
     │     { email, password }                                 │
     ├────────────────────────────────────────────────────────>│
     │                                                          │
     │                                      2. Verify password  │
     │                                         (bcrypt compare) │
     │                                                          │
     │                                      3. Generate tokens: │
     │                                         - Access (15min) │
     │                                         - Refresh (7day) │
     │                                                          │
     │  4. Response:                                            │
     │     - accessToken (in body)                              │
     │     - refreshToken (HttpOnly cookie)                     │
     │<────────────────────────────────────────────────────────┤
     │                                                          │
     │  5. Store accessToken in memory                          │
     │     (NOT localStorage!)                                  │
     │                                                          │
     │  6. Subsequent requests:                                 │
     │     Authorization: Bearer <accessToken>                  │
     ├────────────────────────────────────────────────────────>│
     │                                                          │
     │                                      7. verifyToken:     │
     │                                         - Check signature│
     │                                         - Check expiry   │
     │                                         - Extract user   │
     │                                                          │
     │  8. Response with data                                   │
     │<────────────────────────────────────────────────────────┤
     │                                                          │
     │  ... 15 minutes later ...                                │
     │                                                          │
     │  9. Request with expired token                           │
     ├────────────────────────────────────────────────────────>│
     │                                                          │
     │  10. 401 Unauthorized { expired: true }                  │
     │<────────────────────────────────────────────────────────┤
     │                                                          │
     │  11. POST /api/auth/refresh                              │
     │      (refreshToken sent automatically via cookie)        │
     ├────────────────────────────────────────────────────────>│
     │                                                          │
     │                                     12. Verify refresh   │
     │                                         token            │
     │                                     13. Generate new     │
     │                                         access token     │
     │                                                          │
     │  14. New accessToken                                     │
     │<────────────────────────────────────────────────────────┤
     │                                                          │
     │  15. Retry original request                              │
     │      with new token                                      │
     ├────────────────────────────────────────────────────────>│
     │                                                          │
     │  16. Success                                             │
     │<────────────────────────────────────────────────────────┤
     │                                                          │
```

---

## Authorization Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    REQUEST PROCESSING                            │
└─────────────────────────────────────────────────────────────────┘

  HTTP Request
       │
       ▼
┌─────────────────┐
│  verifyToken    │  ← Middleware 1
│                 │
│  1. Extract JWT │
│  2. Verify sig  │
│  3. Check expiry│
│  4. Set req.user│
└────────┬────────┘
         │
         │ req.user = { userId, email, role, zone }
         ▼
┌─────────────────┐
│   checkRole     │  ← Middleware 2
│                 │
│  1. Check role  │
│  2. Verify      │
│     hierarchy   │
│  3. Check       │
│     permission  │
└────────┬────────┘
         │
         │ (if OPERATOR)
         ▼
┌─────────────────┐
│   checkZone     │  ← Middleware 3 (conditional)
│                 │
│  1. Check if    │
│     OPERATOR    │
│  2. Add zone    │
│     filter      │
│  3. Set         │
│     req.zone    │
│     Filter      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Controller    │  ← Handler
│                 │
│  1. Parse input │
│  2. Validate    │
│  3. Call service│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│    Service      │  ← Business Logic
│                 │
│  1. Additional  │
│     RBAC checks │
│  2. Resource    │
│     ownership   │
│  3. Business    │
│     rules       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Repository    │  ← Data Access
│                 │
│  1. Build query │
│  2. Apply       │
│     filters     │
│  3. Execute     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│    MongoDB      │  ← Database
└─────────────────┘
```

---

## Role Hierarchy

```
                    ┌──────────────────┐
                    │   SUPER_ADMIN    │
                    │   (Level 5)      │
                    │                  │
                    │  • All access    │
                    │  • Configure     │
                    │    thresholds    │
                    │  • Manage users  │
                    └────────┬─────────┘
                             │
                             │ inherits all from below
                             ▼
                    ┌──────────────────┐
                    │ HEAD_SUPERVISOR  │
                    │   (Level 4)      │
                    │                  │
                    │  • Industry-wide │
                    │  • Manage sites  │
                    │  • Assign super  │
                    │    visors        │
                    └────────┬─────────┘
                             │
                             │ inherits all from below
                             ▼
                    ┌──────────────────┐
                    │ SITE_SUPERVISOR  │
                    │   (Level 3)      │
                    │                  │
                    │  • Site-level    │
                    │  • Manage ops    │
                    │  • Resolve       │
                    │    alerts        │
                    └────────┬─────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
       ┌──────────────────┐    ┌──────────────────┐
       │     AUDITOR      │    │    OPERATOR      │
       │   (Level 2)      │    │   (Level 1)      │
       │                  │    │                  │
       │  • Read-only     │    │  • Zone-level    │
       │  • All data      │    │  • Live data     │
       │  • Generate      │    │  • Acknowledge   │
       │    reports       │    │    alerts        │
       └──────────────────┘    └──────────────────┘
```

---

## Data Scope Model

```
┌─────────────────────────────────────────────────────────────────┐
│                      ORGANIZATIONAL HIERARCHY                    │
└─────────────────────────────────────────────────────────────────┘

                         ┌──────────────┐
                         │  Industrie   │  ← Company
                         │  (Company)   │
                         └──────┬───────┘
                                │
                    ┌───────────┼───────────┐
                    │           │           │
                    ▼           ▼           ▼
              ┌─────────┐ ┌─────────┐ ┌─────────┐
              │ Site A  │ │ Site B  │ │ Site C  │  ← Factories
              └────┬────┘ └────┬────┘ └────┬────┘
                   │           │           │
         ┌─────────┼─────┐     │     ┌─────┼─────┐
         │         │     │     │     │     │     │
         ▼         ▼     ▼     ▼     ▼     ▼     ▼
     ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐
     │Zone 1│ │Zone 2│ │Zone 3│ │Zone 4│ │Zone 5│  ← Monitoring Areas
     └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘
        │        │        │        │        │
        ▼        ▼        ▼        ▼        ▼
     ┌──────────────────────────────────────────┐
     │         Sensor Nodes & Sensors           │  ← Devices
     └──────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                        ACCESS SCOPES                             │
└─────────────────────────────────────────────────────────────────┘

SUPER_ADMIN:
  ├── Industrie 1
  │   ├── Site A
  │   │   ├── Zone 1
  │   │   └── Zone 2
  │   └── Site B
  │       └── Zone 3
  ├── Industrie 2
  │   └── Site C
  │       ├── Zone 4
  │       └── Zone 5
  └── ... (ALL)

HEAD_SUPERVISOR (Industrie 1):
  └── Industrie 1
      ├── Site A
      │   ├── Zone 1
      │   └── Zone 2
      └── Site B
          └── Zone 3

SITE_SUPERVISOR (Sites A, B):
  ├── Site A
  │   ├── Zone 1
  │   └── Zone 2
  └── Site B
      └── Zone 3

OPERATOR (Zones 1, 2):
  ├── Zone 1
  │   ├── Sensors
  │   ├── Alerts
  │   └── AI Predictions
  └── Zone 2
      ├── Sensors
      ├── Alerts
      └── AI Predictions

AUDITOR:
  └── ALL (read-only)
```

---

## Permission Check Flow

```
┌─────────────────────────────────────────────────────────────────┐
│              PERMISSION EVALUATION ALGORITHM                     │
└─────────────────────────────────────────────────────────────────┘

Input: user, action, resource

┌─────────────────────┐
│ Is SUPER_ADMIN?     │
└──────┬──────────────┘
       │
       ├─ YES ──> ✅ ALLOW
       │
       └─ NO
          │
          ▼
┌─────────────────────┐
│ Check role          │
│ permissions         │
└──────┬──────────────┘
       │
       ├─ Has permission? ──> YES ──┐
       │                            │
       └─ NO ──> ❌ DENY            │
                                    ▼
                          ┌─────────────────────┐
                          │ Check resource      │
                          │ ownership           │
                          └──────┬──────────────┘
                                 │
                    ┌────────────┼────────────┐
                    │            │            │
                    ▼            ▼            ▼
          ┌──────────────┐ ┌──────────┐ ┌──────────┐
          │HEAD_SUPER    │ │SITE_SUPER│ │OPERATOR  │
          │              │ │          │ │          │
          │industryId    │ │sites     │ │zones     │
          │matches?      │ │Managing  │ │Assigned  │
          │              │ │includes? │ │includes? │
          └──────┬───────┘ └────┬─────┘ └────┬─────┘
                 │              │            │
                 ├─ YES ────────┼────────────┼──> ✅ ALLOW
                 │              │            │
                 └─ NO ─────────┴────────────┴──> ❌ DENY
```

---

## Token Structure

```
┌─────────────────────────────────────────────────────────────────┐
│                      ACCESS TOKEN (JWT)                          │
└─────────────────────────────────────────────────────────────────┘

Header:
{
  "alg": "HS256",
  "typ": "JWT"
}

Payload:
{
  "userId": "507f1f77bcf86cd799439011",
  "email": "operator@enim.tn",
  "role": "OPERATOR",
  "zone": "Zone-A",
  "iat": 1714992000,
  "exp": 1714992900  ← 15 minutes
}

Signature:
HMACSHA256(
  base64UrlEncode(header) + "." +
  base64UrlEncode(payload),
  SECRET_KEY
)

Storage: JavaScript memory (NOT localStorage)
Lifetime: 15 minutes
Usage: Every API request


┌─────────────────────────────────────────────────────────────────┐
│                     REFRESH TOKEN (JWT)                          │
└─────────────────────────────────────────────────────────────────┘

Payload:
{
  "userId": "507f1f77bcf86cd799439011",
  "iat": 1714992000,
  "exp": 1715596800  ← 7 days
}

Storage: HttpOnly cookie (JavaScript cannot read)
Lifetime: 7 days
Usage: Get new access token when expired

Cookie Attributes:
- httpOnly: true     ← XSS protection
- secure: true       ← HTTPS only (production)
- sameSite: 'strict' ← CSRF protection
- path: '/api/auth/refresh'
```

---

## Database Schema Relationships

```
┌─────────────────────────────────────────────────────────────────┐
│                      DATA MODEL                                  │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐
│     User     │
├──────────────┤
│ _id          │
│ username     │
│ email        │
│ password     │◄─── bcrypt hashed
│ role         │◄─── ENUM[5 roles]
│ industryId   │────┐
│ sitesManaging│──┐ │
│ zonesAssigned│─┐│ │
└──────────────┘ ││ │
                 ││ │
                 ││ │
┌────────────────┘│ │
│                 │ │
│  ┌──────────────┘ │
│  │                │
│  │  ┌─────────────┘
│  │  │
▼  ▼  ▼
┌──────────────┐       ┌──────────────┐
│  Industrie   │◄──────│     Site     │
├──────────────┤       ├──────────────┤
│ _id          │       │ _id          │
│ nom          │       │ nom          │
│ secteur      │       │ industrieId  │
│ localisation │       │ supervisorId │───┐
│ actif        │       │ localisation │   │
└──────────────┘       │ actif        │   │
                       └──────────────┘   │
                              │            │
                              │            │
                              ▼            │
                       ┌──────────────┐   │
                       │     Zone     │   │
                       ├──────────────┤   │
                       │ _id          │   │
                       │ code         │   │
                       │ nom          │   │
                       │ siteId       │   │
                       │ industrieId  │   │
                       │ operators    │◄──┘
                       │   Assigned   │
                       │ actif        │
                       └──────────────┘
                              │
                              │
                              ▼
                       ┌──────────────┐
                       │ SensorNode   │
                       ├──────────────┤
                       │ _id          │
                       │ nodeId       │
                       │ zoneId       │
                       │ siteId       │
                       │ status       │
                       └──────────────┘
```

---

## Frontend Component Hierarchy

```
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND ARCHITECTURE                         │
└─────────────────────────────────────────────────────────────────┘

App
 │
 ├─ AuthProvider ◄─── Manages auth state
 │   │
 │   └─ useAuth() hook
 │       ├─ user
 │       ├─ isAuthenticated
 │       ├─ login()
 │       ├─ logout()
 │       └─ refresh()
 │
 ├─ Router
 │   │
 │   ├─ Public Routes
 │   │   └─ /login
 │   │
 │   └─ Protected Routes ◄─── ProtectedRoute wrapper
 │       │
 │       ├─ / (Overview)
 │       │   └─ requires: ['VIEW_KPI']
 │       │
 │       ├─ /alerts
 │       │   └─ requires: ['VIEW_ALERTS']
 │       │
 │       ├─ /reports
 │       │   └─ requires: ['GENERATE_REPORT']
 │       │
 │       ├─ /config
 │       │   └─ role: SUPER_ADMIN
 │       │
 │       └─ /users
 │           └─ role: SUPER_ADMIN
 │
 └─ Components
     │
     ├─ ProtectedRoute
     │   ├─ Check isAuthenticated
     │   ├─ Check role (if specified)
     │   ├─ Check permissions (if specified)
     │   └─ Redirect if unauthorized
     │
     └─ PermissionGate
         ├─ Check user permissions
         ├─ Hide/show children
         └─ Optional fallback UI


Example Usage:

<ProtectedRoute requires={['VIEW_KPI']}>
  <Overview>
    <PermissionGate permission="GENERATE_REPORT">
      <Button>Generate Report</Button>
    </PermissionGate>
  </Overview>
</ProtectedRoute>
```

---

## Security Layers

```
┌─────────────────────────────────────────────────────────────────┐
│                    DEFENSE IN DEPTH                              │
└─────────────────────────────────────────────────────────────────┘

Layer 1: Network
  ├─ HTTPS (TLS 1.3)
  ├─ CORS policy
  ├─ Rate limiting
  └─ IP whitelisting (SUPER_ADMIN)

Layer 2: Authentication
  ├─ JWT tokens
  ├─ bcrypt password hashing
  ├─ HttpOnly cookies
  ├─ Token expiration
  └─ Refresh token rotation

Layer 3: Authorization (Middleware)
  ├─ verifyToken
  ├─ checkRole
  ├─ checkPermission
  └─ checkZone

Layer 4: Business Logic (Services)
  ├─ Resource ownership validation
  ├─ Scope filtering
  ├─ Business rule enforcement
  └─ Data sanitization

Layer 5: Data Access (Repository)
  ├─ Query filtering
  ├─ Field projection
  └─ Aggregation pipelines

Layer 6: Database
  ├─ MongoDB authentication
  ├─ Role-based access
  ├─ Encryption at rest
  └─ Audit logging
```

---

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    ERROR RESPONSES                               │
└─────────────────────────────────────────────────────────────────┘

Request
  │
  ├─ No token
  │   └─> 401 Unauthorized
  │       { message: "Token manquant" }
  │
  ├─ Invalid token
  │   └─> 401 Unauthorized
  │       { message: "Token invalide" }
  │
  ├─ Expired token
  │   └─> 401 Unauthorized
  │       { message: "Token expiré", expired: true }
  │       Frontend: Auto-refresh and retry
  │
  ├─ Wrong role
  │   └─> 403 Forbidden
  │       { message: "Rôle requis: SUPER_ADMIN", yourRole: "OPERATOR" }
  │
  ├─ Missing permission
  │   └─> 403 Forbidden
  │       { message: "Permission refusée: CREATE_USER" }
  │
  ├─ Wrong zone (OPERATOR)
  │   └─> 403 Forbidden
  │       { message: "Accès limité à votre zone" }
  │
  └─ Resource not found
      └─> 404 Not Found
          { message: "Ressource non trouvée" }
```

---

## Audit Trail (Proposed)

```
┌─────────────────────────────────────────────────────────────────┐
│                    AUDIT LOG STRUCTURE                           │
└─────────────────────────────────────────────────────────────────┘

AuditLog {
  _id: ObjectId,
  userId: ObjectId → User,
  action: String,        // "CREATE_USER", "DELETE_SITE", etc.
  resource: String,      // "User:123", "Site:456"
  resourceType: String,  // "User", "Site", "Zone"
  resourceId: String,
  details: {
    before: Object,      // State before change
    after: Object,       // State after change
    changes: [String]    // List of changed fields
  },
  ipAddress: String,
  userAgent: String,
  timestamp: Date,
  success: Boolean
}

Example:
{
  userId: "507f1f77bcf86cd799439011",
  action: "UPDATE_USER",
  resource: "User:507f191e810c19729de860ea",
  resourceType: "User",
  resourceId: "507f191e810c19729de860ea",
  details: {
    before: { role: "OPERATOR" },
    after: { role: "SITE_SUPERVISOR" },
    changes: ["role"]
  },
  ipAddress: "192.168.1.100",
  userAgent: "Mozilla/5.0...",
  timestamp: "2026-05-06T10:30:00Z",
  success: true
}
```

---

**Last Updated:** May 6, 2026  
**Version:** 1.0
