# Production — FastAPI microservice (api.py), inference.py, if_inference.py
# Install: pip install -r requirements.txt
# Notebooks / training plots: pip install -r requirements-notebooks.txt

fastapi>=0.110.0,<1
uvicorn[standard]>=0.27.0,<1
pydantic>=2.0,<3

numpy>=1.26.0,<2
pandas>=2.2.0,<3
scikit-learn>=1.5.0,<2
joblib>=1.4.0,<2
scipy>=1.11.0,<2

# Windows: use the PyPI "tensorflow" wheel (includes CPU). Do not pin tensorflow-intel separately.
tensorflow>=2.16.0,<2.17
